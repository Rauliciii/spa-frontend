export default class HistoryItem {
  constructor(userId, submissionId, price, color, createTime, id) {
    this.userId = userId;
    this.submissionId = submissionId;
    this.price = price;
    this.color = color;
    this.createTime = createTime;
    this.id = id;
  }

}
